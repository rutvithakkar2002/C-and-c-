#include<stdio.h>
#include<conio.h>
struct Node
{
	int info;
	struct Node *next;
}*Head=NULL;
void creaate();
void display();
void del();
void main()
{

}
void create()
{
	if(Head==NULL)
	{
		Head=malloc(sizeof(struct Node));
		Head->info=num;
		Head->next=NULL;
	}
	else
	{
		struct Node *temp;
		temp=malloc(sizeof(struct Node));
		temp->info=num;
		temp->next=NULL;
		Head->next=temp;
	}
}