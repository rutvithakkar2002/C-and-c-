#include<stdio.h>
#include<conio.h>
void main()
{
	int i;
	char ch='A';  //a=96 a=a+1 B
	clrscr();
	for(i=0;i<=25;i++)
	{
		textcolor(i);
		sound(i*100);
		delay(i*20);
		nosound();
		gotoxy(40,i+1);
		cprintf("\n %c",ch+i);
	}
	getch();
f}